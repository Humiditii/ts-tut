// introduction to types

// core types are : string number boolean

function addB(n1: number, n2: number, level: string){
    return n1 + n2;
}

const number1 = 3;
const number2 = 2.99;
const level = '500l'

const result = addB(number1, number2, level)

// const x:string = 'hello' => this is redundant

console.log(result)

// more datatypes
// Object